CREATE TABLE 
junior_da_personal.watching_data AS(
  SELECT *
  FROM `dala-rdcc.mgg_data.watching_data`
)


#спочатку взказуємо назву з нашого датасету або до якого маємо відповідний доступ
#далі вказуємо з якого датасету хочемо скопіювати таблицю і які самі дані, у вигляді підзапиту
